# EduPilot — Python Dependencies
# Install with: pip install -r requirements.txt

# Core
groq>=0.9.0
streamlit>=1.35.0

# Environment variable loading
python-dotenv>=1.0.0

# Vector store (Pinecone)
pinecone>=3.0.0

# Embeddings
sentence-transformers>=3.0.0

# BM25 keyword search
rank-bm25>=0.2.2

# PDF parsing (optional but recommended)
PyMuPDF>=1.24.0

# Word document support (optional)
python-docx>=1.1.0

# Scientific computing
numpy>=1.24.0

# Cross-encoder reranker (optional — improves reranking quality)
# Included via sentence-transformers; install separately only if needed:
# pip install sentence-transformers[cross-encoder]
